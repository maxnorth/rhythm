// Test: Fire-and-forget tasks (no await)
// Expected: All tasks are queued immediately without waiting
workflow(ctx, inputs) {
  Task.run("task1", { "async": true })
  Task.run("task2", { "async": true })
  Task.run("task3", { "async": true })
}
