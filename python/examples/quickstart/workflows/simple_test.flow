let orderId = Inputs.orderId

let sleepTask = Time.delay(10 * 1000)  // 2 minutes

let result1 = await Task.run("process_payment", {
    order_id: "test1",
    amount: 1
})

let task2 = Task.run("process_payment", {
    order_id: "test2",
    amount: 2
})

let result3 = await Task.run("process_payment", {
    order_id: "test3",
    amount: 3
})

let result2 = await task2
await sleepTask


let result4 = await Task.run("process_payment", {
    order_id: "test4",
    amount: 4
})

return {
    orderId: orderId,
    result1: result1,
    result2: result2,
    result3: result3
}
