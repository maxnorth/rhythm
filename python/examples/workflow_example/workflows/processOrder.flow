workflow(ctx, inputs) {
  task("chargeCard", {
      "orderId": "order-123",
      "amount": 99.99
  })
  await sleep(500)
  await task("shipOrder", { "orderId": "order-123" })
  task("sendEmail", { "to": "customer@example.com", "subject": "Order shipped!", "body": "Your order is on the way" })
}
